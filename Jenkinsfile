pipeline {
  agent any
  environment {
        PATH = "/opt/gradle/gradle-6.3/bin:$PATH"
    }
  stages {

   stage('Test') {
        steps {
            echo 'test success'
        }
      }

    stage('Build') {
      steps {

           echo 'build success'

      }
    }
  }
}